# API Specifications & Contracts (`api/`)

This directory contains API contracts, schema definitions, route specifications, and shared type definitions.

## Recommended Structure
- `schemas/`: OpenAPI / Swagger specs, JSON schemas, or GraphQL schemas
- `types/`: Shared interface types and data models
- `routes/`: Endpoint documentation and route definitions
